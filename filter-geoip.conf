
	# enrich public ips with geoloc
	if [saddr] {
		geoip {
			source => "saddr"
			target => "[source][geo]"
			default_database_type => "City"
		}
	}
	if [daddr] {
		geoip {
			source => "daddr"
			target => "[destination][geo]"
			default_database_type => "City"
		}
	}
	if [devicehostip] {
		geoip {
			source => "devicehostip"
			target => "[host][geo]"
			default_database_type => "City"
		}
	}
